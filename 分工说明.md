# 跨境金融业财核算与智能决策平台 — 团队分工说明

> 本文档基于项目实际代码（`backend/pom.xml`、`frontend/package.json`、`application.yml`、12 个 SQL 迁移脚本、`com.finance.platform` 包结构）整理，描述技术栈、数据库表归属、各类人员的职责与跨人员依赖关系。

---

## 一、整体技术栈

### 后端（`backend/`，Spring Boot 单模块）

| 维度 | 选型 | 版本 | 用途 |
|---|---|---|---|
| 框架 | Spring Boot | 3.5.16 | 父 POM，**必须 Java 21**（Spring Boot 4.x 与第三方库不兼容） |
| 语言 | Java | 21 | records / switch 模式匹配 |
| 安全 | spring-boot-starter-security | 跟随 Boot | JWT + 方法级 `@PreAuthorize` |
| Web | spring-boot-starter-web | 跟随 Boot | REST + SSE（`SseEmitter`） |
| 缓存 | spring-boot-starter-data-redis | 跟随 Boot | Lettuce 连接池，汇率内存缓存 + 登录态 |
| AOP | spring-boot-starter-aop | 跟随 Boot | 审计日志切面 |
| 校验 | spring-boot-starter-validation | 跟随 Boot | `@Valid` |
| ORM | mybatis-plus-spring-boot3-starter | 3.5.7 | 分页 / 逻辑删除 / 自动填充 |
| 数据库 | MySQL Connector/J | 跟随 Boot | 8.0+，**utf8mb4** |
| JWT | jjwt-api / impl / jackson | 0.12.6 | 双 token（access + refresh） |
| ETL | EasyExcel | 4.0.3 | Excel 解析 |
| AI | langchain4j + langchain4j-open-ai | 0.35.0 | 对接 DeepSeek（**1.x 有破坏性变更，禁用**） |
| 配置注入 | spring-dotenv | 4.0.0 | 加载 `.env` 中的 `LANGCHAIN_API_KEY` |
| 工具 | Hutool | 5.8.32 | 通用工具 |
| Lombok | 跟随 Boot | — | 实体 |
| 测试 | spring-boot-starter-test + spring-security-test | — | JUnit 5 |

### 前端（`frontend/`，Vite + Vue 3）

| 维度 | 选型 | 版本 |
|---|---|---|
| 框架 | Vue | 3.5.12 |
| 构建 | Vite | 5.4.10 |
| UI | Element Plus + @element-plus/icons-vue | 2.8.4 / 2.3.1 |
| 状态 | Pinia | 2.2.4（含 `store/pageState.js` 跨页状态持久化） |
| 路由 | Vue Router | 4.4.5 |
| HTTP | Axios | 1.7.7（`utils/request.js` 处理 401 刷新 + 重定向锁） |
| 图表 | ECharts | 5.5.1（`components/EchartsWrapper.vue`） |

### 外部依赖

- **MySQL 8.0+**（库 `cross_finance`，utf8mb4_general_ci）
- **Redis 6+**（默认 localhost:6379，db 0）
- **DeepSeek API**（`https://api.deepseek.com/v1`，模型 `deepseek-chat`，超时 60s）
- **Chroma 向量库**（RAG 用，嵌入模型 `bge-small-zh`，D 人员本地起）
- **JDK 21**（编译强制要求，JDK 8 无法编译）

---

## 二、数据库表全景（共 12 张表）

按归属人员分组：

| # | 表名 | 所属脚本 | 主负责 | 说明 |
|---|---|---|---|---|
| 1 | `sys_user` | 01 / 05 | A | 用户，`role_ids` 存 JSON 数组，`dept_id` 关联 sys_dept |
| 2 | `sys_dept` | 05 | A | 部门树，`parent_id` 自关联 |
| 3 | `sys_audit_log` | 01 / 08 | A | 审计日志，08 增加 `old_value` + `undone` 支撑撤销 |
| 4 | `raw_order` | 01 / 11 | B | **平台账单 + 银行流水统一存储**，`source` 区分 PLATFORM/BANK；11 增加对账字段 |
| 5 | `import_template` | 01 / 09 / 10 | B | 字段映射 + 清洗规则配置，`ai_generated` 标识 AI 生成 |
| 6 | `import_batch` | 01 | B | 导入批次状态机（IMPORTED/CLEANING/CLEANED/FAILED） |
| 7 | `extra_cost` | 04 | B | 10 种费用类型枚举，`cny_amount` 后端回填 |
| 8 | `exchange_rate_snapshot` | 01 | C | 汇率快照，`(rate_date, from, to)` 唯一 |
| 9 | `cost_allocation_rule` | 01 | C | 分摊规则，`rule_type` WEIGHT/AMOUNT |
| 10 | `profit_report` | 01 / 12 | C | 利润报表，12 大幅增强（店铺/成本结构/对账/订单时间） |
| 11 | `ai_session` | 04 | D | AI 会话 |
| 12 | `ai_message` | 04 | D | AI 消息，`seq_no` 会话内自增 |

**跨表读写说明：**
- D 的 `AiReportController` 和驾驶舱会**只读** C 的 `profit_report`
- B 的 `DataManagementController` 会**只读/改** C 的 `exchange_rate_snapshot`、`extra_cost.cny_amount`（按汇率重算）
- A 的 `AuditLogAspect` 会**写**所有人员的 `sys_audit_log`

---

## 三、A 人员：系统管理 & 安全基座

### 技术栈重点
Spring Security + JJWT 0.12.6 + Spring AOP + MyBatis-Plus（`MyMetaObjectHandler` 自动填充）+ Redis（可作 token 黑名单）。

### 涉及数据库表（3 张，主负责）

| 表 | 操作 | 关键字段 |
|---|---|---|
| `sys_user` | CRUD / 重置密码 / 分配角色 | `password`(BCrypt) / `role_ids`(JSON) / `dept_id` / `status` |
| `sys_dept` | 树形 CRUD | `parent_id` / `dept_code`(唯一) |
| `sys_audit_log` | 写（AOP 自动）/ 查 / 批量删 / 撤销 | `method` / `params` / `old_value`(撤销快照) / `undone` |

### 类清单（~18 个）

**基础设施包 `config/` + `common/`：**
- `common/utils/JwtUtils.java` — 双 token，`generateAccessToken/RefreshToken`、`isValidAccessToken/RefreshToken`
- `config/JwtAuthenticationFilter.java` — 拒绝 refresh token 滥用
- `config/SecurityConfig.java` — `@EnableMethodSecurity`，放行 `/auth/login` + `/auth/refresh`，`AuthenticationEntryPoint` 返回 401/403
- `common/aspect/AuditLogAspect.java` — `AUDITED_METHODS` 白名单，含 B/C/D 的写接口
- `config/MyMetaObjectHandler.java`
- `config/LangChainConfig.java` — **D 的依赖**，API key 空时返回降级 stub
- `config/ThreadPoolConfig.java` — `etl-*` 给 B，`ai-*` 给 D
- `config/MyBatisPlusConfig.java` — 分页插件
- `config/WebMvcConfig.java`
- `common/core/Result.java` / `BaseEntity.java` / `LoginUser.java`
- `common/exception/BusinessException.java` / `GlobalExceptionHandler.java`

**业务包 `system/`：**
- `controller/AuthController.java`（`/auth/login` `/auth/refresh`）→ `SysUserMapper`
- `controller/SysUserController.java`（类级 `@PreAuthorize("hasRole('ADMIN')")`）→ `SysUserMapper`
- `controller/SysDeptController.java`（`/list` 全员，写操作 ADMIN）→ `SysDeptMapper`
- `controller/SysAuditLogController.java`（仅 ADMIN）→ `SysAuditLogMapper`
- `service/SysUserService.java` / `SysAuditLogService.java` / `UndoService.java` + `mapper/UndoMapper.java`（基于 `old_value` 快照回滚 B 的数据管理操作）

### 前端页面（4 个）
- `views/Login.vue`
- `views/system/UserManage.vue`
- `views/system/AuditLog.vue`
- `views/Forbidden.vue`

---

## 四、B 人员：数据底座（ETL 引擎）

### 技术栈重点
EasyExcel 4.0.3 + 责任链（CleanRule）+ 工厂（FileParserFactory）+ Hutool + `ImportFieldNormalizer`（金额千分位/货币符号/中文单位/括号负数；币种中英文/全角/别名；日期多格式）。

### 涉及数据库表（4 张，主负责）

| 表 | 操作 | 关键字段 |
|---|---|---|
| `raw_order` | 导入 / 查 / 改 / 删 / 对账 | `source`(PLATFORM/BANK) / `clean_status` / `reconcile_status`(0未对账/1已完成/2对账失败/3未到账/4不明入账) / `reconcile_match_id` / `reconcile_diff` |
| `import_template` | CRUD / AI 生成 | `column_mapping`(JSON) / `clean_rules` / `ai_generated` |
| `import_batch` | 写状态机 / 查 | `batch_no`(UUID) / `status` / `error_detail`(JSON) |
| `extra_cost` | 导入 / CRUD / 批量补零 | `cost_type`(10 种枚举) / `cny_amount`(后端按 `cost_date` 汇率回填) / `period` |

> B 还会**只读** C 的 `exchange_rate_snapshot`（折算 CNY）。

### 类清单（~22 个）

**Controller（3 个）：**
- `data/controller/DataImportController.java` — 上传 → 解析 → 清洗 → 入库
- `data/controller/BankFlowController.java` — 银行流水分页 / 批量对账 / `cancelReconcile`
- `data/controller/DataManagementController.java` — **1100+ 行**，账单/流水/额外费用/汇率四类 CRUD + 批量编辑 + Excel 导出 + 批量补零

**ETL 引擎（15 个，`data/etl/` + 解析/规则实现）：**
- 解析层：`FileParser` / `ExcelFileParser`(EasyExcel) / `CsvFileParser`(含 `detectCharset()` 处理 GBK) / `FileParserFactory`（**按扩展名选**，不要用 `getFileType()`）
- 清洗层：`CleanRule` 接口 / `CleanRuleChain` / `DeduplicateRule` / `FilterInvalidRule` / `CurrencyConvertRule` / `DefaultCurrencyRule` / `TrimRule` / `CleanContext`
- 错误：`FailedRow`(rowNo/fieldName/fieldValue/reason/suggestion) / `FieldMappingException`
- 工具：`common/utils/ImportFieldNormalizer.java` / `common/utils/FileValidationUtils.java`（**四重校验**：非空 + 50MB + 扩展名 + Content-Type + magic number）

**Service（8 个）：**
`DataEtlService(Impl)` / `BankReconcileService` / `ExtraCostService` / `ImportBatchService` / `ImportTemplateService` / `ImportTemplateAiService`（调 DeepSeek 推断字段映射）

**Entity/Mapper（6 个）：**
`RawOrder` / `ImportTemplate` / `ImportBatch` / `ExtraCost` / `CostType`(枚举) + 对应 Mapper

### 前端页面（5 个）
- `views/data/BillImport.vue`
- `views/data/BankReconciliation.vue`（纯导入页）
- `views/data/DataManagement.vue`（CRUD + 对账双模式）
- `views/data/ExtraCostImport.vue`
- `views/data/ExchangeRateImport.vue`（**前端归 B，后端调 C 的 `ExchangeRateImportService`**）

---

## 五、C 人员：业财核算 + 汇率 + 完整性

### 技术栈重点
策略模式（`AllocationStrategy`）+ `@Scheduled` 定时任务 + `@PostConstruct` 启动加载 + MyBatis-Plus 聚合查询 + `@Transactional`（核算原子性）。

### 涉及数据库表（3 张，主负责）

| 表 | 操作 | 关键字段 |
|---|---|---|
| `exchange_rate_snapshot` | 导入 / 查 / 缓存刷新 | `rate_date` / `(from,to)` 唯一 / `rate` |
| `cost_allocation_rule` | CRUD / 启停 | `rule_type`(WEIGHT/AMOUNT) / `formula`(JSON) / `enabled` |
| `profit_report` | 重算（先删后插）/ 查 / 导出 | `period`(月份/RANGE) / `fee_cost`+`shared_cost`+`direct_cost`=`cost_amount` / `shop_id` / `reconcile_status` |

> C 的 `profit_report` 会被 D 只读消费（AI 归因 + 驾驶舱）。

### 类清单（~12 个）

**Controller（2 个）：**
- `accounting/controller/ModelConfigController.java` — 分摊规则 CRUD + 启停
- `accounting/controller/ProfitCalcController.java` — `/calculate`(ADMIN/FINANCE) / `/report` / `/calculateByRange` / `/exportReport` / `/trend`（同比 + 多月趋势线，MONTH 模式）

**Service（5 个）：**
- `ProfitEngineService(Impl)` — 幂等（先删旧报表）、按周期聚合 `raw_order` → 取启用规则 → 按策略分摊 `extra_cost` → 算毛利/净利 → `saveBatch`
- `CostAllocationService` — 根据规则选策略
- `ProfitDiagnosisService` — 异常诊断（负利润率 / 成本占比异常 / 汇率波动）
- `DataIntegrityCheckService` — **核算前阻断**：缺失日期的 extra_cost、汇率不齐、银行流水未对账
- `ExchangeRateService(Impl)` / `ExchangeRateImportService` / `ExchangeRateSyncTask`（`@Scheduled` 每日 + `@PostConstruct` 启动加载 + `refreshCache()`）

**Strategy（3 个）：**
`AllocationStrategy` / `WeightAllocationStrategy` / `AmountAllocationStrategy`

**工具：**
`common/utils/CurrencyConvertUtils.java`（B 的 ETL 和 C 的核算共用）

**Entity/Mapper：**
`ProfitReport` / `CostAllocationRule` + Mapper（`ProfitReportMapper` 含 5 个聚合方法供 D 复用）

### 前端页面（2 个）
- `views/accounting/ProfitReport.vue`（统计卡片 + ECharts 趋势 + 同比表 + 明细表，默认上个月）
- `views/accounting/ModelConfig.vue`（运营只读，按钮按角色显隐）

---

## 六、D 人员：AI 智能决策

### 技术栈重点
LangChain4j 0.35.0（`OpenAiChatModel` + `StreamingChatLanguageModel`，**不是 1.x 的 `ChatModel`**）+ `SseEmitter` + `@Tool` 注解（Agent）+ Chroma 向量库 + `bge-small-zh` 嵌入 + `fetch` + `ReadableStream`（前端消费 SSE 带 JWT header）。

### 涉及数据库表（2 张主负责 + 1 张只读）

| 表 | 操作 | 关键字段 |
|---|---|---|
| `ai_session` | CRUD | `user_id` / `title` |
| `ai_message` | 增 / 删 / 编辑后重生 | `session_id` / `role`(USER/ASSISTANT) / `seq_no` |
| `profit_report` | **只读**（归因 + 驾驶舱聚合） | — |

### 类清单（~7 个）

**Controller（2 个）：**
- `ai/controller/AiChatController.java` — 会话 CRUD + `/chat/send` 返回 `SseEmitter`（30s 超时） + 消息编辑/删除/重生 + 多轮上下文（最近 10 条）
- `ai/controller/AiReportController.java` — AI 利润归因

**Service/Agent（4 个）：**
- `AiChatService(Impl)` — 会话/消息持久化
- `AiAnalysisService(Impl)` — 组装 prompt（本期/上期对比、店铺明细）→ DeepSeek → 文字归因
- `RagKnowledgeService(Impl)` — Chroma 向量库检索财务合规知识拼入 prompt
- `ai/agent/AiTools.java` — `@Tool` 注解声明，让大模型调 C 的利润/汇率/报表接口

**Entity/Mapper：**
`AiSession` / `AiMessage` + Mapper

**Prompt 模板：**
`resources/prompt/ProfitAnalysisPrompt.txt`

### 前端页面（2 个）
- `views/ai/AiAdvisor.vue`（三栏布局，打字机效果，DeepSeek 式 UI）
- `views/ai/Dashboard.vue`（KPI + ECharts + 店铺占比，复用 C 的 `ProfitReportMapper`）

---

## 七、跨人员依赖关系（开工顺序）

```
A 先行（基础设施 + 鉴权 + 审计切面）
  └─→ B / C / D 都依赖 Result / BaseEntity / AuditLogAspect / ThreadPoolConfig
  └─→ D 依赖 A 的 LangChainConfig（API key 空时返回降级 stub）

B 与 C 交叉：
  - B 的 DataManagement 改 extra_cost.currency 后调 C 的 ExchangeRateService 重算 cny_amount
  - B 的 ETL 引擎调 C 的 CurrencyConvertUtils
  - ExchangeRateImport.vue 前端归 B，后端 ExchangeRateImportService 归 C

C 与 D 交叉：
  - D 的 AiTools 调 C 的 ProfitCalcController / ExchangeRateService
  - D 的 AiReportController / Dashboard 只读 C 的 profit_report
  - C 的 DataIntegrityCheckService 会读 B 的 raw_order.reconcile_status / extra_cost
```

### 角色权限矩阵（6 种角色）

| 角色 | 代码 | 权限范围 |
|---|---|---|
| 管理员 | `ADMIN` | 全部菜单 |
| 财务 | `FINANCE` | 数据底座 + 业财核算 + 智能决策 |
| 审批经理 | `APPROVER` | 智能决策 |
| 出纳 | `CASHIER` | 智能决策 |
| 运营 | `OPERATOR` | 数据底座（只读） + 业财核算（只读） + 智能决策 |
| 普通员工 | `EMPLOYEE` | 智能决策（驾驶舱 + AI 顾问） |

> 角色代码在 `05_dept_and_roles.sql` 注释中硬编码说明，无需建角色表；`sys_user.role_ids` 存 JSON 数组（如 `["ADMIN","FINANCE"]`）。

---

## 八、关键工程约束（摘自项目记忆）

1. **Spring Boot 3.5.16 + Java 21**，禁用 4.x
2. **LangChain4j 0.35.0**，禁用 1.x（`ChatLanguageModel` 已重命名为 `ChatModel`）
3. **MySQL utf8mb4**，Windows 命令行默认 GBK，SQL 脚本须 `SET NAMES utf8mb4;`
4. **JWT 双 token**：accessToken（2h）+ refreshToken（7d），各自带 `type` claim
5. **JWT secret** 非空且 ≥32 字节，非 dev profile 用默认 secret 会启动失败
6. **文件上传四重校验**：非空 + 50MB + 扩展名(xlsx/xls/csv) + Content-Type + magic number
7. **/auth/refresh** 加入 Security 白名单
8. **JWT 过滤器只接受 accessToken**，拒绝 refreshToken 滥用
9. **审计切面 `AUDITED_METHODS`** 须覆盖所有写接口（含 B 的 9 个 DataManagement 写操作、C 的 `calculateByRange`/`exportReport`、B 的 `cancelReconcile`、A 的 `AuthController.refresh`）
10. **MyBatis-Plus LambdaQueryWrapper** 的 `.ge/.le` 参数在条件判断前求值，提供值的方法须处理 null/empty 防 NPE
11. **FileParser 选择**用 `fileParserFactory.getParserByFileName()` 按扩展名，**不要**用 `template.getFileType()`
12. **CsvFileParser** 须 `detectCharset()` 自动识别 UTF-8/GBK
13. **日期解析**用 `DateTimeFormatterBuilder` 支持 1-2 位月/日/时/分/秒
14. **MyBatis 注解 SQL** 中比较运算符直接用 `<` `>`，不要用 `&lt;` `&gt;` 转义
15. **核算原子性**用 `@Transactional`，预算扣减等并发场景用原子 SQL
16. **利润核算幂等**：先删旧报表再重算
17. **AI 流式输出**用 SSE，30 秒超时保护
18. **会话上下文**：最近 10 条消息，跨会话/设备通过数据库持久化
19. **消息编辑**触发删除后续消息再重生
20. **401 响应**返回 `{code:401, msg:"登录已失效，请重新登录"}`；**403** 返回 `{code:403, msg:"权限不足，无法访问"}`
21. **前端 401 处理**：自动调 `/auth/refresh` 续期，并发请求排队重放，refresh 失败才重定向登录（用 `isRedirecting` 锁防重复）
22. **数据管理页**序号列显示分页连续自增序号（非数据库 ID）
23. **固定类型属性**（平台/币种/数据来源/费用状态等）用可搜索可清空的 `el-select`，不用输入框
24. **页面状态**用 Pinia + localStorage（key: `cross-finance-page-state`）持久化，URL 参数优先级最高
25. **导入模板**含列名 + 红色示例行；导入错误返回 `FailedRow`（含 suggestion）
26. **业务核算**支持自定义日期范围（不止月份），默认核算期间为上个月
